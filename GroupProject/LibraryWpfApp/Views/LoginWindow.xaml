<Window x:Class="LibraryWpfApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LibraryWpfApp.ViewModels"
        Title="📚 Library Management System - Login" 
        Height="650" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F8FAFC">

    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Window.Resources>
        <!-- Drop Shadow Effect -->
        <DropShadowEffect x:Key="CardDropShadow" Color="Black" BlurRadius="20" ShadowDepth="4" Opacity="0.12"/>
        
        <!-- Enhanced Card Style -->
        <Style x:Key="EnhancedCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource CardDropShadow}"/>
        </Style>

        <!-- Simple TextBox Style - No ControlTemplate -->
        <Style x:Key="SimpleLoginTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2563EB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Simple PasswordBox Style - No ControlTemplate -->
        <Style x:Key="SimpleLoginPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2563EB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Enhanced Login Button Style -->
        <Style x:Key="EnhancedLoginButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2563EB" Offset="0"/>
                        <GradientStop Color="#1D4ED8" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#1D4ED8" Offset="0"/>
                                            <GradientStop Color="#1E40AF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E40AF"/>
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Border Style="{StaticResource EnhancedCardStyle}" Padding="40">
            <StackPanel>
                <!-- Header Section -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                    <Border Width="80" Height="80" 
                            Background="{StaticResource PrimaryGradient}" 
                            CornerRadius="40" 
                            Margin="0,0,0,20"
                            Effect="{StaticResource CardDropShadow}">
                        <TextBlock Text="📚" 
                                   FontSize="32" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Welcome Back!" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="#1E293B" 
                               HorizontalAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Sign in to your library account" 
                               FontSize="16" 
                               Foreground="#64748B" 
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Username Section -->
                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="Username" 
                               FontSize="15" 
                               FontWeight="SemiBold" 
                               Foreground="#1E293B"
                               Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource SimpleLoginTextBoxStyle}"/>
                </StackPanel>

                <!-- Password Section -->
                <StackPanel Margin="0,0,0,32">
                    <TextBlock Text="Password" 
                               FontSize="15" 
                               FontWeight="SemiBold" 
                               Foreground="#1E293B"
                               Margin="0,0,0,8"/>
                    <PasswordBox Name="PasswordBox" 
                                 PasswordChanged="PasswordBox_PasswordChanged" 
                                 Style="{StaticResource SimpleLoginPasswordBoxStyle}"/>
                </StackPanel>

                <!-- Login Button -->
                <Button Content="🔑 Sign In" 
                        Command="{Binding LoginCommand}" 
                        Style="{StaticResource EnhancedLoginButtonStyle}"
                        Margin="0,0,0,24"/>

                <!-- Footer -->
                <TextBlock Text="Library Management System v2.0" 
                           FontSize="12" 
                           Foreground="#94A3B8" 
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>


