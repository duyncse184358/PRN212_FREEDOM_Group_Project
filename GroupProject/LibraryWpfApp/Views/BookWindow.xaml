<Window x:Class="LibraryWpfApp.Views.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LibraryWpfApp.ViewModels"
        xmlns:conv="clr-namespace:LibraryWpfApp.Converters"
        Title="Quản lý sách" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">

    <!-- Đã loại bỏ Window.DataContext để dùng DI hoặc code-behind -->

    <Window.Resources>
        <Style x:Key="RightAlignedText" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="300" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" Style="{StaticResource ModernTextBoxStyle}"/>
            <Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>

            <Button Content="Thêm mới" Command="{Binding AddCommand}" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="Sửa đã chọn" Command="{Binding EditCommand}" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="Xóa đã chọn" Command="{Binding DeleteCommand}" Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="Mượn sách" Command="{Binding BorrowBookCommand}" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="Trả sách" Command="{Binding ReturnBookCommand}" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="Đánh dấu mất" Command="{Binding MarkLostCommand}" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="Đánh dấu hư hỏng" Command="{Binding MarkDamagedCommand}" Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Books}"
                  SelectedItem="{Binding SelectedBook, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{StaticResource ModernDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã sách" Binding="{Binding BookId}" Width="Auto"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding Isbn}" Width="Auto"/>
                <DataGridTextColumn Header="Tiêu đề" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Tác giả" Binding="{Binding Author}" Width="*"/>
                <DataGridTextColumn Header="Nhà xuất bản" Binding="{Binding Publisher}" Width="*"/>
                <DataGridTextColumn Header="Năm XB" Binding="{Binding PublicationYear}" Width="Auto"/>
                <DataGridTextColumn Header="Thể loại" Binding="{Binding Category.CategoryName}" Width="Auto"/>
                <DataGridTextColumn Header="Tổng số" Binding="{Binding NumberOfCopies}" Width="Auto"/>
                <DataGridTextColumn Header="Còn lại" Binding="{Binding AvailableCopies}" Width="Auto"/>
                <DataGridTextColumn Header="Vị trí" Binding="{Binding ShelfLocation}" Width="Auto"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
