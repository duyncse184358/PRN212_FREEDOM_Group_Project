<UserControl x:Class="LibraryWpfApp.UserControls.BooksUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Books Management" 
                   FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,20" Foreground="#2c3e50"/>
        
        <!-- Search and Add -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="TxtSearch" Grid.Column="0" 
                     Height="35" VerticalContentAlignment="Center"
                     Padding="10,0" FontSize="14"
                     BorderBrush="#bdc3c7" BorderThickness="1"/>
            
            <Button x:Name="BtnSearch" Grid.Column="1" 
                    Content="🔍 Search" Height="35" Width="80"
                    Background="#3498db" Foreground="White"
                    BorderThickness="0" Margin="10,0,0,0"
                    Click="BtnSearch_Click"/>
            
            <Button x:Name="BtnAddBook" Grid.Column="2" 
                    Content="➕ Add Book" Height="35" Width="100"
                    Background="#27ae60" Foreground="White"
                    BorderThickness="0" Margin="10,0,0,0"
                    Click="BtnAddBook_Click"/>
        </Grid>
        
        <!-- Books List -->
        <DataGrid Grid.Row="2" x:Name="DgBooks"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderBrush="#bdc3c7" BorderThickness="1">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookId}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding Isbn}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Year" Binding="{Binding PublicationYear}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Copies" Binding="{Binding NumberOfCopies}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Available" Binding="{Binding AvailableCopies}" Width="80" IsReadOnly="True"/>
                
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" Width="30" Height="25" 
                                        Background="#f39c12" Foreground="White" 
                                        BorderThickness="0" Margin="0,0,5,0"
                                        Click="BtnEditBook_Click" 
                                        Tag="{Binding BookId}"
                                        ToolTip="Edit"/>
                                <Button Content="🗑️" Width="30" Height="25" 
                                        Background="#e74c3c" Foreground="White" 
                                        BorderThickness="0"
                                        Click="BtnDeleteBook_Click" 
                                        Tag="{Binding BookId}"
                                        ToolTip="Delete"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
